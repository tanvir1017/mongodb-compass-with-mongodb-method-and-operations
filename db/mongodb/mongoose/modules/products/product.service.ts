// Mongoose builtin method for post, get, will be here. e.g products.save()

/*
export const createBookToDB = async(data:IBook):Promise<IBook> => {
    const books = new Book(data);
    // console.log(books)
    // console.log(books.addFileldFuc())
    await books.save();
    return books; 
}
*/ 