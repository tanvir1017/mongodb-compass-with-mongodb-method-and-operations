// All controller function will appear here. we will call the function which is inside service.ts file as req, res
/* export const createBook = async (
  req: Request,
  res: Response,
  next: NextFunction
) => {
  const data = req.body;
  const books = await createBookToDB(data);

  res.status(200).json({
    status: "success",
    data: books,
  });
};
*/ 